kind = "web"
previewPath = "/"
title = "Synorlab Interviewer"
version = "1.0.0"
id = "artifacts/synorlab"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25111

[services.development]
run = "pnpm --filter @workspace/synorlab run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/synorlab", "run", "build" ]
publicDir = "artifacts/synorlab/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25111"
BASE_PATH = "/"
