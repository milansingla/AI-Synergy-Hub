/**
 * Synorlab Runtime Configuration
 * --------------------------------
 * Edit this file directly on your Hostinger server (public_html/config.js)
 * to point the frontend at your deployed Replit backend.
 *
 * After deploying to Replit, your API URL will look like:
 *   https://your-project-name.replit.app
 *
 * Set apiUrl to that value (no trailing slash, no "/api" suffix).
 */
window.__SYNORLAB_CONFIG__ = {
  apiUrl: "", // e.g. "https://synorlab.replit.app"
};
